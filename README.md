# claudemd-lazy-load-references
Keep your CLAUDE.md from eating the context budget — a lazy-load references pattern for Claude Code power users, plus a !-prefix workaround for the self-edit classifier block.
